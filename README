to configure 'Debug':
	cmake -S . -B /path/to/debug/configure
		-DCMAKE_BUILD_TYPE=Debug
		-DCMAKE_TOOLCHAIN_FILE=/path/to/toolchain/file
		-DCMAKE_INSTALL_PREFIX=/path/to/install

to configure 'Release':
	cmake -S . -B /path/to/release/configure
		-DCMAKE_BUILD_TYPE=Release
		-DCMAKE_TOOLCHAIN_FILE=/path/to/toolchain/file
		-DCMAKE_INSTALL_PREFIX=/path/to/install

to build 'Debug' and install:
	cmake --build /path/to/configure --config=Debug
		--prefix=/path/to/install
		--target install

to build 'Release' and install:
	cmake --build /path/to/configure --config=Release
		--prefix=/path/to/install
		--target install
